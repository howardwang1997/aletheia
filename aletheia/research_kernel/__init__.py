"""Authority boundary for the graph-scoped scientific kernel.

PR-0 intentionally exposes no behavior from this package.  Its presence makes the dependency
boundary non-vacuous before the first kernel reducer and command schemas land in PR-1.
"""
