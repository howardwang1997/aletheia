"""The coder: an Opus worker authors a constrained model ``solution.py`` that the
fixed honest-evaluation harness runs. A static AST gate + a restricted subprocess
keep AI-authored code from reaching the network/filesystem or running away."""
